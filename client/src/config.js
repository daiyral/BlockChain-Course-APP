
module.exports = {
    env: {
        API_KEY: "", // put your infura api key here (or different provider)
        environment: "dev", // "dev" for local development, "staging" for deployment (for staging must have api key.)
    }
}